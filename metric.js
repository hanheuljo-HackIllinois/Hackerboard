function byteToDollar(bytes) {
	return (bytes / 1024).toFixed(2);
}

function byteToHDD(bytes) {
	return ((bytes / 1024) / 1024).toFixed(1);
}

//	DNA Data Storage? Prob not. too small?

//	First Android Smartphone has 256mb storage, how may phones, maybe

//	one movie? 1.4gigs / 700mb

//	how many books can you write?

//	cd roms, dvds, floppy lol

//	songs (3.5mb)

//	ios9 (1.3gb)

//	average web page (1600kb)

//	find characters per page, and compute # pages, compute weight, height, etc. of the stack of papers